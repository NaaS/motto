# This example gives an "Undershot the scope?" error during lexing,
# since the line starting with "c" doesn't move back to a known scope.
#type t1 : record
#  a : record
#    b : boolean
#   c : integer

